load('num.mat');
index = randperm(length(num),50);
result = num(index);
aaa = zeros(length(result),1);
for i = 1:length(result)
    m = result(i);
    aaa(i) = m()
end
for i =1:5
    p = result((1+(i-1)*10):i*10);
    save(['pqfile' num2str(i) '.txt'],'p','-ascii')
end

